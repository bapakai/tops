TOPSID v1.5.1 — Haiku Vision

UPLOAD / REPLACE:
1. app/api/analyze/route.ts
2. package.json

The package.json is based on the currently audited TOPSID package.json,
with only @anthropic-ai/sdk added.

Vercel Environment:
ANTHROPIC_API_KEY = existing Anthropic API key
Optional:
TOPSID_VISION_MODEL
ANTHROPIC_VISION_MODEL

Do NOT upload .env or API keys to GitHub.
